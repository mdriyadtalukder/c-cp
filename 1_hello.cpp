#include <iostream> // Used for input and output (like cout, cin).
#include <conio.h> //Used for console functions like getch() (mostly in old/DOS-based compilers).
using namespace std; //Allows using standard library names without writing std::.,,,like allowing you to write cout instead of std::cout.
main()
{
    cout << "hello \n world" << endl;
    cout << "Hi";
    //getch();
    return 0;
}