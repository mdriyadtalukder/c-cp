#include <iostream>
using namespace std;
int main(){
    char l='A';
    if(l=='a' || l=='i' || l=='e' || l=='o' || l=='u' || l=='A' || l=='I' || l=='E' || l=='O' || l=='U'){
        cout<<"Vowel";
    }
    else{
        cout<<"Alphabet";
    }

    //  char i = 'i'; // the character to check
    // string vowels = "aioue"; // string of vowels

    // // check if i is in vowels
    // if (vowels.find(i) != string::npos) {  // find() returns npos if not found  ....npos is a special constant used with strings to indicate “not found.”
    //     cout << "vowel" << endl; 
    // } else {
    //     cout << "Not vowel" << endl; 
    // }
}